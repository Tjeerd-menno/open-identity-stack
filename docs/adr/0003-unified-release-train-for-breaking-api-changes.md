# Unified release train for breaking admin API changes

OpenIdentityStack allows non-versioned breaking changes on the Admin API, but only when the API, shared frontend client, and Management Web are released in a coordinated release train. We keep this policy to preserve delivery speed without API version proliferation while acknowledging tight coupling between backend and frontend operator workflows.
