# Non-Authoritative Spec Archive

This directory is retained only as historical Copilot design context.

The accepted implementation source for the application permission registry is `specs/006-application-permission-registry/`. Do not use the files in this directory as the governing spec, plan, task list, contract, or quickstart for new implementation work.
